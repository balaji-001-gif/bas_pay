from setuptools import setup, find_packages

setup(
    name='payease_erpnext',
    version='0.0.1',
    description='PayEase Mobile Payment Integration for ERPNext',
    author='PayEase',
    author_email='support@payease.com',
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
